import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Header = () => {
  return (
    <View
      style={{
        height: 80,
        width: '100%',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>Vietcourse</Text>
    </View>
  );
};

export default Header;

const styles = StyleSheet.create({});
